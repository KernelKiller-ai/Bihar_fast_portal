name: Keep Render Backend Alive

on:
  schedule:
    # Har 14 minute me run hoga
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: |
          curl -s https://bihar-fast-portal.onrender.com/api/notices > /dev/null
          echo "Pinged Render successfully!"